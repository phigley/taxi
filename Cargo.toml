[package]
name = "taxi"
version = "0.1.0"
authors = ["Peter Higley <phigley@gmail.com>"]

[lib]
name = "taxi"

[dependencies]
rand = "0.5"
serde = "1.0"
serde_derive = "1.0"
toml = "0.4"
rayon = "1.0"
float-cmp = "0.3"
enum-map = "0.2"

[target.'cfg(not(windows))'.dependencies]
tui = "0.2"
termion = "1.5"

[dev-dependencies]
assert_matches = "1.2"
criterion = "0.2"

[[bin]]
name = "taxi"
test = false

[[bench]]
name = "training_session"
harness = false